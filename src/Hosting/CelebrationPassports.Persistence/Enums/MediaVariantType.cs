namespace CelebrationPassports.Persistence.Enums;

// ERD doesn't enumerate values for this one — starter tier set, easy to extend.
public enum MediaVariantType
{
    Thumbnail = 1,
    Medium = 2,
    Original = 3
}
