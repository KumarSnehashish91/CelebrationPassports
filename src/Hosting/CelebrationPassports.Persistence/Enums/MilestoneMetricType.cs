namespace CelebrationPassports.Persistence.Enums;

// ERD doesn't enumerate values for this one — covers the two obvious progress shapes.
public enum MilestoneMetricType
{
    Count = 1,
    Boolean = 2
}
