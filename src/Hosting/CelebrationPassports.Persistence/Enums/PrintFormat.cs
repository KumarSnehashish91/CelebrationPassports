namespace CelebrationPassports.Persistence.Enums;

// Gift Story print preview (gift-story-print-preview.md). Trim sizes/pricing live in
// the Application layer (GiftStoryService) — this enum is just the selector.
public enum PrintFormat
{
    LifeBook = 1,
    PassportEdition = 2
}
